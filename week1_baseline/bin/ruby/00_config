#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
STEP_DIR="$(cd "$SCRIPT_DIR/../../ruby/00_config" && pwd)"

cd "$STEP_DIR"
exec bundle exec ruby examples/example.rb